---
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: {{ rancherk8s_type }}-cilium
  namespace: kube-system
spec:
  valuesContent: |-
{% if rancherk8s_loadbalancer_ip is defined%}
    k8sServiceHost: {{ rancherk8s_loadbalancer_ip }} # K8s loadbalancer IP for HA
{% else %}
    k8sServiceHost: 127.0.0.1
{% endif %}
    k8sServicePort: 6443
    kubeProxyReplacement: true
    bgp:
      enabled: false
    hubble:
      enabled: true
      relay:
        enabled: true
      ui:
        enabled: true
    image:
      pullPolicy: IfNotPresent
    operator:
      replicas: {{ groups['server'] | length | int }}
{% if rancherk8s_cni_l2_enabled %}
    l2announcements:
      enabled: true
{% endif %}
{% if rancherk8s_cni_gateway %}
    gatewayAPI:
      enabled: true
      default: true
    hostServices:
      enabled: true
    envoy:
      enabled: true
      securityContext:
        capabilities:
          keepCapNetBindService: true
          envoy:
            - BPF
            - CAP_NET_ADMIN
            - CAP_SYS_ADMIN
            - NET_ADMIN
            - NET_BIND_SERVICE
            - SYS_ADMIN
{% endif %}
